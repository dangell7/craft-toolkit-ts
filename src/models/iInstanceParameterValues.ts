import { iInstanceParameterValue } from './iInstanceParameterValue'

export class iInstanceParameterValues {
  values: iInstanceParameterValue[]

  constructor(values: iInstanceParameterValue[]) {
    this.values = values
  }
}
